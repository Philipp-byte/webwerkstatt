{
  "id": "03-klick-zaehler",
  "title": "Mini-Projekt: Klick-Zähler",
  "steps": [
    {
      "type": "explain",
      "text": "Dein erstes Mini-Projekt! Du kombinierst **alles** aus dem JavaScript-Teil:\n\nEine **Variable** merkt sich den Zählerstand. Bei jedem **Klick** wird sie um 1 erhöht und auf der Seite **angezeigt**:\n\n```js\nzaehler = zaehler + 1;\nanzeige.textContent = zaehler;\n```\n\n`zaehler = zaehler + 1` bedeutet: „Nimm den alten Wert, zähle 1 dazu, speichere das Ergebnis zurück.“ So funktionieren Likes, Warenkörbe und Highscores – überall zählt eine Variable mit."
    },
    {
      "type": "example",
      "text": "So soll es am Ende funktionieren – klicke ein paarmal auf **+1**! Schau dir den Code genau an, gleich baust du ihn selbst.",
      "html": "<h1>Klick-Zähler</h1>\n<button id=\"knopf\">+1</button>\n<p id=\"anzeige\">0</p>\n",
      "css": "#anzeige { font-size: 40px; font-weight: bold; }\nbutton { font-size: 20px; padding: 8px 20px; }\n",
      "js": "let zaehler = 0;\nlet knopf = document.getElementById(\"knopf\");\nlet anzeige = document.getElementById(\"anzeige\");\n\nknopf.addEventListener(\"click\", function () {\n  zaehler = zaehler + 1;\n  anzeige.textContent = zaehler;\n});\n",
      "editable": ["js"]
    },
    {
      "type": "code",
      "task": "Baue den Klick-Zähler selbst – das JS-Feld ist diesmal **leer**:\n\n1. Lege eine Variable `zaehler` mit dem Wert **0** an\n2. Hole Button (`id=\"knopf\"`) und Anzeige (`id=\"anzeige\"`)\n3. Bei jedem Klick: `zaehler` um 1 erhöhen und in der Anzeige zeigen\n\nDer Test klickt dreimal – danach muss die Anzeige **3** zeigen.",
      "starter": {
        "html": "<h1>Klick-Zähler</h1>\n<button id=\"knopf\">+1</button>\n<p id=\"anzeige\">0</p>\n",
        "css": "#anzeige { font-size: 40px; font-weight: bold; }\nbutton { font-size: 20px; padding: 8px 20px; }\n",
        "js": "// Baue hier deinen Klick-Zähler\n"
      },
      "editable": ["js"],
      "hints": [
        "Beginne mit let zaehler = 0; und hole dann beide Elemente mit getElementById.",
        "Im Event-Listener: zaehler = zaehler + 1; und dann anzeige.textContent = zaehler;",
        "```js\nlet zaehler = 0;\nlet knopf = document.getElementById(\"knopf\");\nlet anzeige = document.getElementById(\"anzeige\");\n\nknopf.addEventListener(\"click\", function () {\n  zaehler = zaehler + 1;\n  anzeige.textContent = zaehler;\n});\n```"
      ],
      "tests": [
        { "type": "text", "selector": "#anzeige", "expected": "0", "label": "Vor dem Klicken zeigt die Anzeige 0" },
        { "type": "action", "action": "click", "selector": "#knopf" },
        { "type": "action", "action": "click", "selector": "#knopf" },
        { "type": "action", "action": "click", "selector": "#knopf" },
        { "type": "text", "selector": "#anzeige", "expected": "3", "label": "Nach drei Klicks zeigt die Anzeige 3" },
        { "type": "source", "file": "js", "matches": "let\\s+zaehler", "label": "Eine Variable zaehler wird angelegt" }
      ]
    }
  ]
}
